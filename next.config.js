/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // This line is removed to enable server-side rendering.
  // assetPrefix: './', // If you are deploying to a subdirectory, you might need this.
  // trailingSlash: true, // If you are deploying to a static host, you might need this.
};

export default nextConfig;
